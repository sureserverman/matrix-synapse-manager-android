---
description: Use when testing on emulators, taking screenshots, starting/stopping the emulator stack, spinning up containers, or running any emulator/screenshot flow.
globs: []
alwaysApply: false
---

Read and follow the skill file at `.cursor/skills/android-mcp-orchestrator/SKILL.md` before starting emulators, capturing screenshots, or running MCP operations.

MCP compose root: `~/dev/mcp/android-emu-mcp/`
MCP endpoint: `http://localhost:8000/mcp` (405 on GET = up)
